{
  "name": "virtual_game_api",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "watch": "npx esbuild src/**/* --outdir=dist --platform=node --format=cjs --watch",
    "build": "npx esbuild src/index.ts --outfile=dist/index.js --platform=node --format=cjs --bundle --minify --sourcemap",
    "start": "node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "@types/uuid": "^10.0.0",
    "@types/ws": "^8.5.12",
    "esbuild": "^0.23.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "uuid": "^10.0.0",
    "ws": "^8.18.0"
  }
}
